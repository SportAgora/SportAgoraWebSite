<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/validar_ingresso.css">
    <link rel="stylesheet" href="/css/base.css">
    <title>Validador de Ingressos</title>
</head>
<body>
    <section class="container-ver-eventos">
            <button class="botao-seta" onclick="window.history.back()">
              <img src="/imagens/left-arrow.png" alt="Seta">
            </button>
            <span class="ver-eventos">Voltar</span>
        </section>
        
    <section id="reader"></section>

    <script src="https://unpkg.com/html5-qrcode" type="text/javascript"></script>
    <script>
    // Pega evento_id injetado pelo controller
    const evento_id = <%= evento_id %>;
    console.log("Evento ID:", evento_id);

    function onScanSuccess(decodedText, decodedResult) {
        console.log(`QR Code lido: ${decodedText}`);

        // Extrair o inscricao_id do QR code
        const inscricao_id = decodedText.match(/IngressoID:(\d+)/)[1];

        // Enviar para o backend com evento_id
        fetch('/validar-ingresso', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({ inscricao_id, evento_id })
        })
        .then(res => res.json())
        .then(data => alert(data.message))
        .catch(err => console.error(err));
    }

    var html5QrcodeScanner = new Html5QrcodeScanner(
        "reader", { fps: 10, qrbox: 250 }
    );
    html5QrcodeScanner.render(onScanSuccess);
    </script>
</body>
</html>
